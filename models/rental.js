var movie = require('../models/movies')
var app = require("../app");
var db = app.get("db");

// Constructor function
var Rental = function(id) {
  this.id = id;
};

// Instance functions


// Class Functions


module.exports = Account;
